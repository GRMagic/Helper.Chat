# Testes iniciais com o SemanticKernel

Requer o ollama rodando em algum lugar, pode ser no docker
```
docker run --gpus all -d -v ollama_data:/root/.ollama -p 11434:11434 --name ollama ollama/ollama:latest
```

